## Print this on start so I know it's loaded.
cat("Loading custom .Rprofile")

.First <- function(){
  #plotly api keys
  Sys.setenv("plotly_username"="jaskonas")
  Sys.setenv("plotly_api_key"="g6pOI0NzGvd7XcwWelOS")
  cat("\nPlot.ly profile loaded") 
}
